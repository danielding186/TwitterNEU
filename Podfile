# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterNEU' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterNEU
  pod "KeychainSwift"
  pod "FirebaseStorage"
  pod 'Firebase/Analytics'
  pod "FirebaseAuth"
  pod "FirebaseDatabase"
  pod "Alamofire"
  pod "SwiftyJSON"
end
